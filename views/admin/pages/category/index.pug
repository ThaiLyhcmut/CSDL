extends ../../layouts/default.pug
include ../../mixins/box-head.pug

block main
  .container.my-3
    .row
      .col-12
        +box-head(pageTitle)
      .col-4.text-right 
        a(href=`${prefixAdmin}/products-category/create`
              class="btn btn-outline-success"
            ) + Them moi
    .row
      table(
        class="table table-hover table-sm"
      )
        thead
          tr
            th STT
            th name 
            th Số lượng bài đăng
            th Hành động
        tbody
          each item, index in careers
            tr
              td #{index + 1}
              td #{item.name}
              td #{1}
              td
                a(
                  class="btn btn-info btn-sm"
                  href=`${prefixAdmin}/recruit/detail/${item.id}`
                ) Chi tiết
                a(
                  class="btn btn-warning btn-sm ml-1"
                  href=`${prefixAdmin}/recruit/edit/${item.id}`
                ) Sửa
                button(
                  class="btn btn-danger btn-sm ml-1"
                  item-id=item.id
                  button-delete
                  data-path=`${prefixAdmin}/recruit/delete`
                ) Xóa
          
      